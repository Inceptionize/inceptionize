language: node_js
node_js:
  - 12
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s 
  - export PATH="$HOME/.yarn/bin:$PATH"
cache:
  yarn: true
jobs:
  include:
    - stage: build
      script: yarn install
    - stage: test
      script: yarn test